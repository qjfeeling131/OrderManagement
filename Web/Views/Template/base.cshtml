@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="~/Css/Bootstrap/css/bootstrap.min.css" rel="stylesheet" />
    <script data-main="@Url.Content("~/Js/main.js")" src="~/Js/require.js"></script>
    <title>
        @RenderSection("title", false)
    </title>
    @RenderSection("script", false)

    <style>
        .marginbottom50 {
            margin-bottom: 50px;
        }
    </style>
</head>
<body>
    <div class="maxWidth">
        <div id="dialog_content"></div>
        <section>
            <div class="navbar navbar-inverse  navbar-fixed-top" role="navigation">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <span><img src="~/img/me.svg" width="50" height="50" /></span>
                    </div>
                    <div id="navbar" class="navbar-collapse collapse">
                        <ul class="nav navbar-nav">
                            <li class="active"><a href="~/Home/index">Home</a></li>
                            <li class=""><a href="~/Demo/Index">Demo</a></li>
                            <li class=""><a href="~/lab/index">Lab</a></li>
                        </ul>

                        <form class="navbar-form navbar-right">
                            @*@if (FrontendCore.Utilities.Tools.DistrubutedCache.Get("User")==null)
                            {
                                <input id="logIn" type="button" value="Log In" class="btn btn-success" />
                            }
                            else
                            {
                                <label class="label">@FrontendCore.Utilities.Tools.DistrubutedCache.Get("User")</label> 
                                <input id="SignOut" type="button" value="Sign Out" class="btn btn-success" />
                            }*@
                            @if (Session["User"] == null)
                            {
                                <input id="logIn" type="button" value="Log In" class="btn btn-success" />
                            }
                            else
                            {
                                <label class="label">@Session["User"]</label>
                                <input id="SignOut" type="button" value="Sign Out" class="btn btn-success" />
                            }


                            <input id="MessageMe" type="button" value="Message Me" class="btn btn-primary" />

                        </form>
                        


                    </div>
                </div>
            </div>

        </section>
        <div class="marginbottom50"></div>
        @RenderSection("body", false)
    </div>
</body>

</html>
